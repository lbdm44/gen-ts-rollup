import fs from 'fs-extra';

/**
 * TODO: create tmp dirs with `tmp` and generate the rollup.ts files there.
 * TODO: create a tmp dir within the package that the <package>.rollup.d.ts file will end up in
 */

export function createTmpDirs(packagePaths: string[]) {
  packageP;
}
